nav(class="navbar")

  div(class="navbar__inner")

    +logotype({ class: 'navbar__logotype' })
    +nav-menu(null, { class: 'navbar__nav-menu' })

    div(class="navbar__btns")

      +search(null, null, { class: 'navbar__search' })
      +nav-links(null, { class: 'navbar__nav-links' })